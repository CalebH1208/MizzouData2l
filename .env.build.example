## Cloud credentials injected at build time via -ldflags.
## Copy this to .env.build and fill in real values.
## .env.build is gitignored — never commit it.

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=mizzou-racing-telemetry
AWS_REGION=us-east-2
